{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended", "github>DeadNews/renovate-config"],
  "packageRules": [
    {
      "addLabels": ["main"],
      "description": "Group httpx and pytest-httpx in one PR",
      "groupName": "httpx and pytest-httpx",
      "matchManagers": ["poetry"],
      "matchPackageNames": ["httpx", "pytest-httpx"]
    }
  ]
}
